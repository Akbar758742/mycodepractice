#include<stdio.h>
int main()// assigning a sum of two floating values
{
    float x,y,sum;
    int z;
    printf("enter two float value:");
    scanf("%f%f",&x,&y);
    sum=x+y;
    z=sum;
    printf("the assigning integer value of two flaot numbers is %d",z);
    return 0;
}
